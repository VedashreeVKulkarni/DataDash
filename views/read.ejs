<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Docuent</title>
</head>
<body>
    
    <div class="w-full min-h-screen bg-zinc-800 !p-5 text-white ">
        <div>
            <a class="text-blue-400 " href="/">Back to home</a>
        </div>
        <h1 class="text-3xl tracking-tighter !mt-10  bg-zinc-800 !pb-2 text-white flex ">All Users</h1>
        <div class="users flex flex-wrap  gap-4">
            <% if(users.length > 0){ %>
    
            <% users.forEach(function(user) { %>
            <div class="user !w-72 !mt-15 !p-4 bg-zinc-700 rounded-lg">
                <div class="w-full h-72 rounded-lg bg-zinc-500 overflow-hidden">
                    <img class="w-full h-full object-cover object-top" src="<%= user.image %>" alt="<%= user.name %>">
                </div>
                <h3 class="text-xl  !mt-3 tracking-tighter"><%=  user.name %></h3>
                 <h5 class="text-zinc-300"><%= user.email %></h5>
                 <div class="flex gap-3 !mt-10">
                    <a class="text-zinc-300 !pr-25" href="/edit/<%= user._id %>">Edit User</a>
                    <a class=" text-red-400 tracking-tighter" href="/delete/<%= user._id %>">Delete User </a>
                 </div>
            </div>
            <% }); %>
             <% } else { %>
            <p class="text-zinc-300 !mt-3">No users found !</p>
            <% } %>
        </div>
       
           

        </div>
    </div>    

</body>
</html>